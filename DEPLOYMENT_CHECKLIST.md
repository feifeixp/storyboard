# 部署检查清单

**项目**: Visionary Storyboard Studio  
**版本**: v2.0  
**日期**: 2025-02-06

---

## ✅ 部署前检查

### 1. 代码质量
- [x] 所有TypeScript类型错误已修复
- [x] 所有功能测试通过
- [x] 无严重bug
- [x] 代码已提交到版本控制

### 2. 配置文件
- [x] `.env.local` 配置正确
- [x] `vite.config.ts` 配置正确
- [x] `package.json` 依赖版本正确
- [x] `tsconfig.json` 配置正确

### 3. 文档完整性
- [x] README.md 更新
- [x] PROJECT.md 完整
- [x] DEVELOPMENT_LOG.md 最新
- [x] PROJECT_DELIVERY.md 已创建
- [x] FINAL_SUMMARY.md 已创建

### 4. 规则库
- [x] `.augment/rules/` 完整 (50KB)
- [x] `docs/rules/` 完整 (105KB)
- [x] 所有规则文件已验证

---

## 🚀 本地部署

### 步骤1: 环境准备
```bash
# 检查Node.js版本 (需要 v18+)
node --version

# 检查npm版本
npm --version
```

### 步骤2: 安装依赖
```bash
# 安装所有依赖
npm install

# 验证安装
npm list --depth=0
```

### 步骤3: 配置API Key
```bash
# 创建 .env.local 文件
cat > .env.local << EOF
VITE_OPENROUTER1_API_KEY=sk-or-v1-...
VITE_DEEPSEEK_API_KEY=sk-...
VITE_GEMINI_API_KEY=...
EOF
```

### 步骤4: 启动开发服务器
```bash
# 启动
npm run dev

# 验证
# 打开浏览器访问 http://localhost:5173
```

### 步骤5: 功能测试
- [ ] 创建项目
- [ ] 上传剧本
- [ ] 剧本分析
- [ ] 生成分镜
- [ ] 角度自动修复
- [ ] 生成九宫格
- [ ] 导出剧本模板

---

## 🌐 生产部署

### 方案1: Vercel部署

#### 步骤1: 安装Vercel CLI
```bash
npm install -g vercel
```

#### 步骤2: 登录Vercel
```bash
vercel login
```

#### 步骤3: 部署
```bash
# 首次部署
vercel

# 生产部署
vercel --prod
```

#### 步骤4: 配置环境变量
```bash
# 在Vercel Dashboard中配置
# Settings → Environment Variables
VITE_OPENROUTER1_API_KEY=sk-or-v1-...
VITE_DEEPSEEK_API_KEY=sk-...
VITE_GEMINI_API_KEY=...
```

#### 步骤5: 验证部署
- [ ] 访问部署URL
- [ ] 测试所有功能
- [ ] 检查API调用
- [ ] 检查性能

---

### 方案2: Netlify部署

#### 步骤1: 构建生产版本
```bash
npm run build
```

#### 步骤2: 安装Netlify CLI
```bash
npm install -g netlify-cli
```

#### 步骤3: 登录Netlify
```bash
netlify login
```

#### 步骤4: 部署
```bash
# 首次部署
netlify deploy

# 生产部署
netlify deploy --prod
```

#### 步骤5: 配置环境变量
```bash
# 在Netlify Dashboard中配置
# Site settings → Environment variables
VITE_OPENROUTER1_API_KEY=sk-or-v1-...
VITE_DEEPSEEK_API_KEY=sk-...
VITE_GEMINI_API_KEY=...
```

---

### 方案3: GitHub Pages部署

#### 步骤1: 构建生产版本
```bash
npm run build
```

#### 步骤2: 配置base路径
```javascript
// vite.config.ts
export default defineConfig({
  base: '/visionary-storyboard-studio/',
  // ...
});
```

#### 步骤3: 部署到GitHub Pages
```bash
# 安装gh-pages
npm install -g gh-pages

# 部署
gh-pages -d dist
```

#### 步骤4: 配置GitHub仓库
- Settings → Pages
- Source: gh-pages branch
- 保存

---

## 🔍 部署后验证

### 功能测试
- [ ] 创建项目功能正常
- [ ] 剧本分析功能正常
- [ ] 分镜生成功能正常
- [ ] 九宫格生成功能正常
- [ ] 导出功能正常

### 性能测试
- [ ] 页面加载速度 <3秒
- [ ] 剧本分析速度 <10秒
- [ ] 分镜生成速度 <60秒
- [ ] 九宫格生成速度 <120秒

### 兼容性测试
- [ ] Chrome浏览器正常
- [ ] Firefox浏览器正常
- [ ] Safari浏览器正常
- [ ] Edge浏览器正常

### API测试
- [ ] OpenRouter API调用正常
- [ ] DeepSeek API调用正常 (可选)
- [ ] Gemini API调用正常 (可选)

---

## 📊 监控指标

### 性能监控
- 页面加载时间
- API响应时间
- 错误率
- 用户活跃度

### 使用监控
- 日活跃用户数
- 项目创建数
- 分镜生成数
- 九宫格生成数

---

## 🐛 常见问题

### 问题1: API Key无效
**症状**: 提示"API Key无效"或"401 Unauthorized"
**解决方案**:
1. 检查 `.env.local` 文件是否存在
2. 检查API Key是否正确
3. 检查API Key是否有效期内
4. 重启开发服务器

### 问题2: 构建失败
**症状**: `npm run build` 报错
**解决方案**:
1. 删除 `node_modules` 和 `package-lock.json`
2. 重新安装依赖: `npm install`
3. 检查TypeScript错误: `npm run build`
4. 修复所有错误后重新构建

### 问题3: 部署后功能异常
**症状**: 本地正常，部署后功能异常
**解决方案**:
1. 检查环境变量是否配置
2. 检查base路径是否正确
3. 检查浏览器控制台错误
4. 检查网络请求是否成功

---

## 📞 技术支持

### 文档资源
- [项目交付文档](./PROJECT_DELIVERY.md)
- [完整项目文档](./PROJECT.md)
- [开发日志](./DEVELOPMENT_LOG.md)

### 问题反馈
- 查看开发日志了解已知问题
- 参考功能验证清单进行测试
- 查阅规则库了解核心规范

---

**检查日期**: 2025-02-06  
**检查人**: AI Assistant  
**状态**: ✅ 通过


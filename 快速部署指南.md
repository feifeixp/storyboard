# 快速部署指南 - Cloudflare Pages

## 🚨 立即修复部署错误

你遇到的错误：
```
✘ [ERROR] Missing entry-point to Worker script or to assets directory
```

### 解决方案（3选1）

#### 方案1：修改 Cloudflare Pages 设置（最简单）

1. 登录 Cloudflare Pages 控制台
2. 进入你的项目设置
3. 找到 **Build & deployments** → **Build configurations**
4. 修改配置：
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
   - **Deploy command**: **删除或留空**
5. 保存并重新部署

#### 方案2：使用 wrangler.toml（已为你创建）

项目中已经创建了 `wrangler.toml` 文件，内容：
```toml
name = "visionary-storyboard-studio"
compatibility_date = "2026-02-06"

[site]
bucket = "./dist"
```

提交这个文件到 Git，然后重新部署即可。

#### 方案3：修改部署命令

在 Cloudflare Pages 设置中，将部署命令改为：
```bash
npx wrangler deploy --assets=./dist
```

---

## 🔑 API Key 配置（外网部署）

### 步骤1：获取 API Keys

#### OpenRouter（必需）

1. 访问 https://openrouter.ai/
2. 注册账号
3. 充值（建议 $10-20）
4. 在 **Keys** 页面创建 API Key
5. 复制 Key（格式：`sk-or-v1-xxxxxxxx...`）

#### DeepSeek（可选，更便宜）

1. 访问 https://platform.deepseek.com/
2. 注册账号
3. 充值（非常便宜，¥1/百万tokens）
4. 创建 API Key
5. 复制 Key（格式：`sk-xxxxxxxx...`）

### 步骤2：在 Cloudflare Pages 设置环境变量

1. 进入 Cloudflare Pages 项目
2. 点击 **Settings** → **Environment variables**
3. 点击 **Add variable**
4. 添加以下变量：

| Variable name | Value | 说明 |
|--------------|-------|------|
| `VITE_OPENROUTER1_API_KEY` | `sk-or-v1-xxxxxxxx...` | 必需 |
| `VITE_DEEPSEEK_API_KEY` | `sk-xxxxxxxx...` | 可选 |
| `VITE_GEMINI_API_KEY` | `xxxxxxxx...` | 可选 |

5. 选择 **Production** 和 **Preview** 环境
6. 点击 **Save**
7. 重新部署项目

---

## ⚠️ 重要安全警告

### 当前架构的问题

你的 API Key 会**暴露在浏览器中**！

任何人都可以：
1. 打开浏览器开发者工具
2. 查看网络请求
3. 看到你的 API Key
4. 盗用你的 Key，产生高额费用

### 临时解决方案

1. **设置使用限额**
   - 在 OpenRouter 控制台设置每日/每月限额
   - 建议设置为 $50/月

2. **监控使用量**
   - 定期检查 OpenRouter 使用情况
   - 发现异常立即删除 Key

3. **定期更换 Key**
   - 每周或每月更换一次 API Key

### 长期解决方案（推荐）

使用 Cloudflare Worker 作为 API 代理：

1. 创建 Cloudflare Worker
2. 将 API Key 存储在 Worker 环境变量中
3. 前端通过 Worker 调用 API
4. Worker 验证请求来源

详细步骤见：`CLOUDFLARE_DEPLOYMENT_GUIDE.md`

---

## 📝 完整部署流程

### 1. 本地测试

```bash
# 安装依赖
npm install

# 创建 .env.local 文件
echo "VITE_OPENROUTER1_API_KEY=sk-or-v1-your-key-here" > .env.local

# 本地运行
npm run dev

# 本地构建测试
npm run build
npm run preview
```

### 2. 推送到 GitHub

```bash
git add .
git commit -m "Add Cloudflare deployment config"
git push origin main
```

### 3. Cloudflare Pages 部署

1. 登录 Cloudflare Pages
2. 连接 GitHub 仓库
3. 配置构建设置：
   - Framework: **Vite**
   - Build command: `npm run build`
   - Build output: `dist`
4. 添加环境变量（见上文）
5. 点击 **Save and Deploy**

### 4. 验证部署

1. 访问部署的网站
2. 测试登录功能
3. 测试分镜生成功能
4. 检查浏览器控制台是否有错误

---

## 🎯 快速检查清单

- [ ] 已修复部署错误（删除 Deploy command 或添加 wrangler.toml）
- [ ] 已获取 OpenRouter API Key
- [ ] 已在 Cloudflare Pages 设置环境变量
- [ ] 已设置 API 使用限额
- [ ] 已测试部署的网站
- [ ] 已了解 API Key 安全风险

---

## 💡 费用估算

### OpenRouter 费用（使用 Gemini 3 Flash Preview）

- **输入**: $0.50 / 百万 tokens
- **输出**: $3.00 / 百万 tokens

**实际使用**：
- 生成30个镜头的分镜 ≈ 50,000 tokens
- 费用 ≈ $0.15 - $0.30 / 次

**建议充值**: $10-20（可用几个月）

### DeepSeek 费用（更便宜）

- **输入**: ¥1 / 百万 tokens
- **输出**: ¥2 / 百万 tokens

**实际使用**：
- 生成30个镜头的分镜 ≈ 50,000 tokens
- 费用 ≈ ¥0.05 - ¥0.10 / 次

**建议充值**: ¥10-20（可用很久）

---

**需要帮助？** 查看完整文档：`CLOUDFLARE_DEPLOYMENT_GUIDE.md`

